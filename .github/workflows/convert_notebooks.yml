name: Build Notebooks

on:
  push:
    paths:
      - 'notebooks/**'
  workflow_dispatch:

jobs:
  convert:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Jupyter
        run: |
          python -m pip install --upgrade pip
          pip install jupyter nbconvert

      - name: Convert notebooks recursively to markdown
        run: |
          find notebooks -name "*.ipynb" | while read nb; do
            echo "Converting $nb"
            jupyter nbconvert --to markdown "$nb" --output-dir="content/markdown/"
          done

      - name: Gather notebook media
        run: |
          find notebooks -type d -name "media" -exec sh -c 'cp -r "$1"/. content/markdown/media/' _ {} \;

      - name: Commit generated markdown
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add content/markdown/*.md
          git add content/markdown/media
          git commit -m "Auto-build notebook markdown" || echo "No changes to commit"
          git push
